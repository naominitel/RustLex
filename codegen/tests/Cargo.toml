[package]
name = "test"
version = "0.0.0"
build = "build.rs"

[build-dependencies]
rustlex_codegen = { version = "*", path = ".." }
syntex          = { version = "0.58.1" }

[dependencies]
rustlex_codegen = { version = "*", path = ".." }
